# Core dependencies
cryptography==45.0.4
requests>=2.31.0
gradio>=4.18.0
python-dotenv>=1.0.1

# Optional cloud LLM support (Modal)
modal-client>=0.57.1

# For static analysis and logging (if extended)
pylint>=3.1.0
rich>=13.7.0

# For parsing and code handling
astor>=0.8.1

# Optional dev/test tooling
pytest>=8.2.0
